#include <stdio.h>
#include <stdlib.h>

int main(){
    int price=500;
    int pay;
    printf("pls enter your pay amount(you have to pay 500): ");
    scanf("%d",&pay);
    if (pay<500)
    {
        printf("not enough money");
    }
        if else (pay>500)
        {
            pay-500=
        }
        

    return 0;
}